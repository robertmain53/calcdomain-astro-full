---
import Base from '../../../layouts/BaseLayout.astro';
const title = `/) { sub(/<title>.*<\/title>/, "<title>" esc(title_txt) "`;
const description = `[^`;
const canonical = "/other/template-home/";
const jsonLd = {"@context": "https://schema.org", "@type": "WebApplication", "name": "/) { sub(/<title>.*<\\/title>/, \"<title>\" esc(title_txt) \"", "url": "https://calcdomain.com/other/template-home/", "applicationCategory": "Calculator", "operatingSystem": "All", "description": "[^"};
---
<Base {title} {description} {canonical} jsonLd={jsonLd}>
<nav class="breadcrumbs"><a href="/">Home</a> › <a href="/other/">Other</a> › <span>/) { sub(/<title>.*<\/title>/, "<title>" esc(title_txt) "</span></nav>
<h1>/) { sub(/<title>.*<\/title>/, "<title>" esc(title_txt) "</h1>
<div class="eeat">
  <div class="item"><strong>Author</strong><br>CalcDomain Editorial</div>
  <div class="item"><strong>Reviewed</strong><br>Expert-verified</div>
  <div class="item"><strong>Last updated</strong><br>2025-10-26</div>
  <div class="item"><strong>Category</strong><br>Other</div>
</div>
<section><h2>About this calculator</h2><p>[^</p></section>
<section><h2>How it works</h2><p>Inputs, formulas, and outputs will be implemented here. All calculations run fully client-side.</p></section>
</Base>
